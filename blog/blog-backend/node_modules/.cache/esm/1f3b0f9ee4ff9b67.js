let mongoose;_e4f‍.x([["default",()=>_e4f‍.o]]);_e4f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const PostSchema = new Schema({
  title: String,
  body: String,
  tags: [String],
  publishedDate: {
    type: Date,
    default: Date.now
  },
  user: {
    _id: mongoose.Types.ObjectId,
    username: String
  }
});

const Post = mongoose.model('Post', PostSchema);
_e4f‍.d(Post);
